import 'package:flutter/material.dart';

class FromListPage extends StatelessWidget {
  const FromListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
